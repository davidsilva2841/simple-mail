import * as api from '../../s'
import { createSlice } from "@reduxjs/toolkit";

const filtersSlice = createSlice({
  name: 'filters',
  initialState: {},
  reducers: {}
});


export function createFilter () {
  return async (dispatch, getState) => {
  
  }
}

